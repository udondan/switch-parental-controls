{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "nintendo": {
      "type": "local",
      "command": [
        "uvx",
        "--from",
        "switch-parental-controls",
        "mcp",
      ],
      "enabled": true,
      "environment": {
        // Set your Nintendo session token here, or leave empty and use
        // the switch_get_login_url / switch_complete_login tools to authenticate.
        "SWITCH_PARENTAL_CONTROL_SESSION_TOKEN": "",
        // Optional: set your timezone (IANA format, e.g. "America/New_York")
        "SWITCH_PARENTAL_CONTROL_TIMEZONE": "Europe/London",
        // Optional: set your language code (e.g. "en-US")
        "SWITCH_PARENTAL_CONTROL_LANG": "en-GB",
      },
    },
  },
}
